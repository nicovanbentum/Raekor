#include "pch.h"
#include "app.h"

int main(int argc, char** argv) {
    
    auto app = Raekor::Application();
    glm::vec2 a;
    std::cout << a.length() << std::endl;
    app.run();

    return 0;
}